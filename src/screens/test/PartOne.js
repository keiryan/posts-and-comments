import CardGallery from "../../components/Card/card";
import localPlaceholderData from "../../localPlaceholderData";

export default function PartOne() {
  return <CardGallery posts={localPlaceholderData} />;
}
